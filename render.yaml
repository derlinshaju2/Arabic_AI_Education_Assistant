services:
  - type: web
    name: Arabic_AI_Education_Assistant
    runtime: python
    env: python-3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT